"""Integration test placeholder — drift detection end-to-end."""

# Populated in Phase 6.
# Tests will inject synthetic data, run the drift detector,
# and assert that the trigger threshold fires correctly.
