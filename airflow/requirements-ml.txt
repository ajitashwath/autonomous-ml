# Dependencies for the ML tasks the retrain DAG runs (train, validation gate, deploy).
# Installed into an isolated virtualenv (see airflow/Dockerfile) because Airflow 2.9 pins
# SQLAlchemy<2.0, while the application code is written for SQLAlchemy 2.x.
# These tasks need no database driver: MLflow is reached over HTTP.
scikit-learn==1.4.2
xgboost==2.0.3
pandas==2.2.2
numpy==1.26.4
scipy==1.13.0
pyarrow==15.0.2
mlflow==2.13.0
pydantic==2.7.1
pydantic-settings==2.2.1
tenacity==8.3.0
structlog==24.1.0
PyYAML==6.0.1
